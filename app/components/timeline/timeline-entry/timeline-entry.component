// component style
import style from './timeline-entry.component.less'

<timeline-entry>
  <article class="timeline-entry { pull-left: opts.entry.align==='left', pull-right: opts.entry.align==='right' }" >
    <div class="te-info">
      <p>{ opts.entry.title }</p>
      <p>{ opts.entry.date }</p>
    </div>
    <div class="te-icon">
      <i class="{ opts.entry.icon }"></i>
    </div>
  </article>
</timeline-entry>
